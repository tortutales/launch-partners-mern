// @scripts
import globals from '../../styles/globals';

export default theme => (Object.assign({}, globals(theme), {
    usersPage: {
    },
    bigAvatar: {
        height: 120,
        width: 120
    }
}));
